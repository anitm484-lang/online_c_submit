# include<stdio.h>
int main()
{
    int n;
    printf("enter the number:");
    scanf("%d",&n);
    printf(n>0? "positive":"negative");  /* this type is only for the one conditon that if else if one is right
                                                    then other is wrong*/
    return 0;
}