#include<stdio.h>    // this is called globally decleare here we can use for function as well as for hall program .
struct student{
int age;
char name[45];
};




#include<stdio.h>
struct emp{
	int age;
	char name[45];
	char address[50];
};
int main()
{
	int grade;       // this is called locally declare because here we do only for the function 
}

