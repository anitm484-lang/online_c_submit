#include<stdio.h>
int main()
{
    int age;
    printf("enter your age:");
    scanf("%d",&age);
    if(age>=18)
    {
        printf("you are eligable for voting ");
    }
    else 
    printf("you are not eligable for voting");
return 0;
}
